/ {
    behaviors {
        hm_qsemi: homerow_qsemi {
        compatible = "zmk,behavior-hold-tap";
        label = "HOMEROW_QSEMI";
        #binding-cells = <2>;
        tapping-term-ms = <220>;
        quick-tap-ms = <0>;
        flavor = "tap-preferred";
        global-quick-tap;
        bindings = <&kp>, <&kp>;
        };

        hm_wy: homerow_wy {
        compatible = "zmk,behavior-hold-tap";
        label = "HOMEROW_WY";
        #binding-cells = <2>;
        tapping-term-ms = <220>;
        quick-tap-ms = <0>;
        flavor = "tap-preferred";
        global-quick-tap;
        bindings = <&kp>, <&kp>;
        };

        hm_fu: homerow_fu {
        compatible = "zmk,behavior-hold-tap";
        label = "HOMEROW_FU";
        #binding-cells = <2>;
        tapping-term-ms = <170>;
        quick-tap-ms = <0>;
        flavor = "tap-preferred";
        global-quick-tap;
        bindings = <&kp>, <&kp>;
        };

        hm_tn: homerow_tn {
        compatible = "zmk,behavior-hold-tap";
        label = "HOMEROW_TN";
        #binding-cells = <2>;
        tapping-term-ms = <150>;
        quick-tap-ms = <0>;
        flavor = "tap-preferred";
        global-quick-tap;
        bindings = <&kp>, <&kp>;
        };

        hm_gh: homerow_gh {
        compatible = "zmk,behavior-hold-tap";
        label = "HOMEROW_GH";
        #binding-cells = <2>;
        tapping-term-ms = <160>;
        quick-tap-ms = <0>;
        flavor = "tap-preferred";
        global-quick-tap;
        bindings = <&mo>, <&kp>;
        };

        th_ht: th_ht {
        compatible = "zmk,behavior-hold-tap";
        label = "TH_HT";
        #binding-cells = <2>;
        tapping-term-ms = <150>;
        quick-tap-ms = <0>;
        flavor = "tap-preferred";
        global-quick-tap;
        bindings = <&mo>, <&kp>;
        };

    };
};
